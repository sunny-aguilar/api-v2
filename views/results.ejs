<h1>Results Page</h1>

<div class="container"></div>

<% data['Search'].forEach(function(movie) { %>
    <ul>
        <li>
            <strong>
                <%= movie['Title'] %>
            </strong>
            - <%= movie['Year'] %>
        </li>
    </ul>
<% }); %>

<a href="/">Search Again</a>